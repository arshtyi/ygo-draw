# Scripts

This directory is organized by responsibility:

- `asset_tools/`: asset download/import tooling and its Python project files
- `postgresql/`: local PostgreSQL start/stop helpers for Windows + Scoop
